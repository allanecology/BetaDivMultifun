---
title: "Functions Processing for BetaDivMultifun"
output:
  pdf_document: default
  html_notebook: default
---
This Vignette relies on the dataset `raw_functions` which is read in and constructed with `1read_raw_dataset.Rmd` and `2calc_raw_dataset`. 

# Select functions for BetaDivMultifun project
```{r}
# waiting for following functions
# "N_loss_comb", 
funselect <- c()
```


# check distributions
normality?

log transform non-normally distributed ones

check distributions again